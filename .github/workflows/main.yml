# .github/workflows/main.yml (FINAL)

name: Playwright Tests CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  # ===============================================
  # 1. JOB DE PRUEBAS DE UI (PARALELO)
  # ===============================================
  ui-tests:
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        project: [ui-chromium, ui-firefox]

    runs-on: ubuntu-latest

    # ✅ INYECCIÓN DE SECRETOS DE UI
    env:
      USER_EMAIL: ${{ secrets.UI_USER_EMAIL }} # SIN ESPACIOS
      USER_PASSWORD: ${{ secrets.UI_USER_PASSWORD }}

    steps:
      - name: ⬇️ Checkout code
        uses: actions/checkout@v4

      - name: 🛠️ Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🌐 Install Playwright browsers
        run: npx playwright install --with-deps

      - name: ▶️ Run UI Tests (${{ matrix.project }})
        # Los tests acceden a las credenciales vía process.env
        run: npx playwright test --project=${{ matrix.project }}

      - name: 📊 Upload Report Artifact (${{ matrix.project }})
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: html-report-${{ matrix.project }}
          path: playwright-report/
          retention-days: 30

  # ===============================================
  # 2. JOB DE PRUEBAS DE API (SECUENCIAL)
  # ===============================================
  api-tests:
    timeout-minutes: 5
    runs-on: ubuntu-latest

    needs: [ui-tests]

    # ✅ INYECCIÓN DE SECRETOS DE API
    env:
      USER_NAME: ${{ secrets.API_USERNAME }}
      PASSWORD: ${{ secrets.API_PASSWORD }}

    steps:
      - name: ⬇️ Checkout code
        uses: actions/checkout@v4

      - name: 🛠️ Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🌐 Install Playwright browsers
        run: npx playwright install --with-deps

      - name: ▶️ Run API Tests (Sequential)
        run: npx playwright test --project=api-mode

      - name: 📊 Upload API Report Artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: html-report-api
          path: playwright-report/
          retention-days: 30
