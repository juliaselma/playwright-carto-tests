# .github/workflows/main.yml (Inyección a nivel de Paso - Versión Final)

name: Playwright Tests CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  # ===============================================
  # 1. JOB DE PRUEBAS DE UI (PARALELO)
  # ===============================================
  ui-tests:
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        project: [ui-chromium, ui-firefox] 
    
    runs-on: ubuntu-latest
    
    # Lógica de Seguridad para evitar fallos en PRs de forks.
    if: github.event_name == 'push' || github.repository == github.event.pull_request.head.repo.full_name

    steps:
      - name: ⬇️ Checkout code
        uses: actions/checkout@v4

      - name: 🛠️ Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🌐 Install Playwright browsers
        run: npx playwright install --with-deps

      # ✅ INYECCIÓN DE SECRETOS A NIVEL DE PASO
      - name: ▶️ Run UI Tests (${{ matrix.project }})
        run: npx playwright test --project=${{ matrix.project }}
        env:
          USER_EMAIL: ${{ secrets.UI_USER_EMAIL }}
          USER_PASSWORD: ${{ secrets.UI_USER_PASSWORD }}

      - name: 📊 Upload Report Artifact (${{ matrix.project }})
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: html-report-${{ matrix.project }} 
          path: playwright-report/ 
          retention-days: 30

  # ===============================================
  # 2. JOB DE PRUEBAS DE API (SECUENCIAL)
  # ===============================================
  api-tests:
    timeout-minutes: 5
    runs-on: ubuntu-latest
    
    needs: ui-tests 
    
    # Lógica de Seguridad
    if: github.event_name == 'push' || github.repository == github.event.pull_request.head.repo.full_name

    steps:
      - name: ⬇️ Checkout code
        uses: actions/checkout@v4

      - name: 🛠️ Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🌐 Install Playwright browsers
        run: npx playwright install --with-deps

      # ✅ INYECCIÓN DE SECRETOS A NIVEL DE PASO
      - name: ▶️ Run API Tests (Sequential)
        run: npx playwright test --project=api-mode
        env:
          USER_NAME: ${{ secrets.API_USERNAME }}
          PASSWORD: ${{ secrets.API_PASSWORD }}

      - name: 📊 Upload API Report Artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: html-report-api
          path: playwright-report/
          retention-days: 30
